public class Data
{
  //User data
  private String firstName, lastName, UUID;
  
  public Data(String firstName, String lastName, String UUID)
  {
      this.firstName = firstName;
      this.lastName = lastName;
      this.UUID = UUID;
  }
}
